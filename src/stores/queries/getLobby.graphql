query lobby($id: String!) {
  party(id: $id) {
    id
    name
    distance
    coverImageUrl
    date
    elevationGain
    group {
      name
    }
    location
    interested {
      id
      email
      avatarUrl
    }
  }
}
