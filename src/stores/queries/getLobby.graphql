query lobby($id: String!) {
  party(id: $id) {
    id
    name
    distance
    coverImageUrl
    date
    elevationGain
    group {
      name
    }
    location
    safetyInstructions
    creator {
      email
    }
    interested {
      id
      email
      avatarUrl
    }
  }
}
