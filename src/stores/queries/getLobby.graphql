query lobby($id: String!) {
  party(id: $id) {
    id
    name
    distance
    coverImageUrl
    date
    elevationGain
    group {
      name
    }
    location
    safetyInstructions
    interested {
      id
      email
      avatarUrl
    }
  }
}
