query lobby($id: String!) {
  party(id: $id) {
    id
    name
    distance
    imageUrl
    date
    elevationGain
    group {
      name
    }
    interested {
      id
      email
      avatarUrl
    }
  }
}
