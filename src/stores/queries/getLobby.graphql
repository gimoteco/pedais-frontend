query lobby($id: String!) {
  party(id: $id) {
    id
    name
    distance
    coverImageUrl
    date
    elevationGain
    group {
      name
    }
    interested {
      id
      email
      avatarUrl
    }
  }
}
